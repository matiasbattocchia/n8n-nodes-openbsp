# Screenshots

Drop the following PNGs here; the main README references them by these exact
filenames. Suggested capture: n8n in your browser at the relevant step, plus the
OpenBSP web UI for the last one.

| File | What it should show |
| ---- | ------------------- |
| `01-install-community-node.png` | n8n **Settings → Community Nodes**, installing `n8n-nodes-openbsp`. |
| `02-create-credential.png` | The **OpenBSP API** credential dialog (API key field + pre-filled publishable key / project URL). |
| `03-send-text-node.png` | An **OpenBSP** node set to **Message → Send Text** with the Account picker, To, and Text fields. |
| `04-echo-bot-workflow.png` | The echo-bot canvas: **OpenBSP Trigger → OpenBSP (Send Text)** connected. |
| `05-execution-output.png` | A trigger execution showing the captured incoming message (the `data` / `context` output). |
| `06-openbsp-conversation.png` | The conversation in the OpenBSP web UI showing the inbound message and the automated reply. |

Optional extras: the **trigger events** dropdown (Conversation / Message /
Message Status) and the **action operations** grouped by resource.
